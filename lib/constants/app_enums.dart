enum BusinessInfoType { main, base, plans, contacts, restriction, toCover }

enum RestrictionType { whole, main, children, adults, parents }

enum DetailsType { spouse, beneficiaries, toInsure, contacts }

enum AlertTypes { error, success, information }

enum MembershipDialogTypes { create, view, premium, claim }
